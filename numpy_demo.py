#numpy :
# python does not support matrix by default.
# to use matrix in python, we need numpy.
# numpy by deafult not available.
# we need to install numpy pkg explicitly.

import numpy as np
arr = np.array([10,20,30,40,50])
print(arr,arr.dtype,arr.shape,arr.ndim)
